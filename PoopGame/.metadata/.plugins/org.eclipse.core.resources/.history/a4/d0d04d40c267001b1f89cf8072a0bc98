package poopgame.ui;

public class MainMenu extends MenuPanel {
	
	public MainMenu() {
		super("wrap 1, fill", "[grow, fill]", "[grow, bottom][grow, top]");
		
		MenuButton createButton = new MenuButton("CREATE GAME");
		createButton.addActionListener(e -> SwingFrame.goTo(new ChampSelection()));
		add(createButton);
		add(new MenuButton("JOIN GAME"));
	}
}
